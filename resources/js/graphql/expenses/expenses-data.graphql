query expenseSelectData {
    paymentMethods: paymentMethods(orderBy: {
        field: NAME,
        order: ASC
    }) {
        id
        name
    }
    categories: categories(orderBy: {
        field: NAME,
        order: ASC
    }) {
        id
        name
    }
}
